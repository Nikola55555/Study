# import sys
# print(sys.__doc__) # Вывод фактического описания модуля
# print(map.__doc__)
#
# """PyDoc"""
#
# py -3 -m pydoc -b  # Команда для запуска сервера из командной строки для отображения информации о документации
#
# dir(x)   # Получение списка атрибутов, присоединенных к данному объекту
# [a for a in dir(x) if not a.startswith('__')]  # отфильтровываем список атрибутов с __ подчеркиваанием
S = 'assddsld'
a = 0
# for i in S:
#     print(ord(i))
#     a += ord(i)
# print(a)
# map(ord, S)  # не работает так
# [ord(i) for i in S]  # не работает так

# for i in range(50):
#     print('hello %d\n\a' % i)




